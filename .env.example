# Postgres connection for the API
DATABASE_URL=postgres://postgres:postgres@localhost:5432/blast_radius

# API port
PORT=8080

# Optional: run the integration suite against an existing Postgres instead of
# spinning one up with Testcontainers (useful for fast local runs).
# TEST_DATABASE_URL=postgres://localhost:5432/blast_radius_test
